# ID del sitio en Umami Cloud (https://cloud.umami.is → Settings → Websites).
# Solo se inyecta en build de producción. Sin esta variable el sitio no carga analytics.
# En producción: Vercel → Environment Variables → PUBLIC_UMAMI_ID (no GitHub Actions).
PUBLIC_UMAMI_ID=
